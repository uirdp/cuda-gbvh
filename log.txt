26/1/12
seg faultはなくなった、けど絵が出てこないrendererが間違っているかbvhが間違っているflattenが間違っているかも
解決した、ただしmodify sceneをすることでtriangle offsetの値がおかしくなっている
（中でflatten nodeを呼んでいるため、modifyするたび大きくなってしまう）
なのでflatten nodeの仕様を変える必要がある

26/1/17
Deleteが呼ばれるようになったが、objectが見つからない
みつかった、triangleを更新する必要がある

26/1/23
並列化：gridにコードを割り振る→leafにコードを割り振る→gpuでagcする(ここまで２月中)→残りの部分と接続する
→直列と計測する

leafにセットするのはdeleteとinsertの二つでよいか聞く

26/2/4
gridにコード割り振る（済）
leafにコードを割り振る（済）
dirtyを集める（済）
gpuに転送
agc
接続
計測

26/2/11
gpuに転送中、結局leafにtriangleを持たせる。また最初の一フレーム分しかやってない

26/2/13
gridにコード割り振る（済）
leafにコードを割り振る（済）
dirtyを集める（済）
gpuに転送 (済)
agc
接続
計測

26/2/17
agc

gridでソート（済）
find nn 
merge
compaction

結局、前フレームのBVHから、グリッドコードをインデックスに親ノードを取得して、
その子（dirtyの兄弟ノード）をcluster_listに入れてagcすれば再接続の必要はない（自動で接続されるはず）

26/2/18
ところが、同じグリッドコードを持つbvh_nodeが複数存在するはず...
これをどうしようか

自分と同じコードでnon_dirtyがあればリストに追加、自分と同じ親のクラスターも追加
親のgrid_codeのbvh treeletのルートへのポインタを取得、左右を確認し、違うidかつnon-dirtyなら追加、もしidが同じなら、さらにその左右を確認するを繰り返す
つまり、共通の親grid_codeを持つ、すべてのclusterをaffectedに追加する

3/13
ミスってなければもうちょっと、ほぼ確実にミスってるけど
あとはBVHを構築するコードを書くだけ、あとCPU版も残さないといけないので、traverse_bvhもCPU版を復活させること
copy_sceneもCPU版を残しておくこと

3/17
コンパイルエラーは消えたが、予想通り木ができていない
終了後のクラスター数がdirty leavesと（おそらく）一致しているので、まったくマージができていない。
liftができていなかったので修正、してもうまくいかない、keyが同一になっていないようだ

3/18
どうやらleafのaabbが初期化できていないらしい
でもCPU版では動いてた・・・どういうことや？
AABBはbuild_bvhで計算していて、グリッドの時点ではまだ計算してないみたい。

26/4/18
affected clusters実装
expiredは未実装
ポリゴンが変なのでaffected clustersがうまくいっていない可能性が高い
